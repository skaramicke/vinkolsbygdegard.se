// src/patches/stega.js
// This is a simple implementation to replace the problematic module

export function stegaEncode(content, data) {
  // A simple implementation that could be improved later
  return content;
}

export function stegaDecode(content) {
  // A simple implementation that could be improved later
  return {};
}

export const isImmutableMap = (obj) => false;
export const isImmutableList = (obj) => false;

export function encodeEntry(entry) {
  // A simple implementation that could be improved later
  return entry;
}
